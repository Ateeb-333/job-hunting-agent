# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Notes: outputs/ and tracker/ are intentionally NOT ignored —
# they are required deliverables per the lab specification.

# Local zip archives / extracted snapshots (not part of the project)
job-hunting-agent.zip
/job-hunting-agent/

# Tracker auto-backup file (regenerated on every write)
tracker/applications.csv.bak

# Status history log is local-state, not a deliverable
tracker/status_history.log

# Local secrets — NEVER commit your API key
.env
.streamlit/secrets.toml
