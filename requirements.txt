# Core agent runs on Python 3.9+ standard library only.
# Optional dependencies enable extra features:

pypdf>=4.0          # PDF input reading (input_jobs/, input_resumes/, input_kb/)
fpdf2>=2.7          # PDF export of the final report
streamlit>=1.30     # Web UI: drag-drop uploads, click-to-analyze, tracker forms
pandas>=1.5         # Used by the Streamlit dashboard for tables & charts
openai>=1.40        # OpenRouter (OpenAI-compatible) for LLM-tailored cover letter,
                    # interview questions, and resume rewrites — see .env.example

# Job-discovery feature (Phase 13). All optional — job_search degrades gracefully
# if any are missing, just skipping the providers that need them.
requests>=2.31      # HTTP client for all job-board API + HTML fetches
beautifulsoup4>=4.12  # HTML parsing for Pakistan job boards (Rozee, Mustakbil, etc.)
feedparser>=6.0     # RSS feeds (Hacker News "Who is hiring", We Work Remotely, etc.)
